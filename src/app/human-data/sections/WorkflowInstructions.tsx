export default function WorkflowInstructions() {
  return (
    <div className="prose prose-slate max-w-none">
      <p>
        We convert briefs into structured rubrics with exemplars and
        anti‑exemplars, then instrument the interface to capture rationale and
        edge cases. Every instruction change is versioned and A/B tested on a
        small batch.
      </p>
    </div>
  );
}

