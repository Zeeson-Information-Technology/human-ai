export default function WorkflowPipeline() {
  return (
    <div className="prose prose-slate max-w-none">
      <p>
        Tasks flow through creation → review → calibration → acceptance. For
        multilingual or code tasks, we assign language/domain‑matched reviewers.
        Metrics (agreement, error types, rework rate) gate progression.
      </p>
    </div>
  );
}

