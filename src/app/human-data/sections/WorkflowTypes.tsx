export default function WorkflowTypes() {
  return (
    <div className="prose prose-slate max-w-none">
      <ul>
        <li>Alignment data (instruction following, refusal, tool‑use)</li>
        <li>Rubric evaluations (pairwise, scalar, and reasoning checks)</li>
        <li>RAG curation (source selection, chunking, citation audits)</li>
        <li>Speech: transcription, diarization, and accent‑fair evaluations</li>
      </ul>
    </div>
  );
}

