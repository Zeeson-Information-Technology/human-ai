export default function TipsStats() {
  return (
    <div className="prose prose-slate max-w-none">
      <p>
        Ask for agreement metrics, inter‑rater reliability, and fraud controls
        (honeypots, copy‑leak checks, behavioral flags). Require sample audits
        and error‑type breakdowns — not just averages.
      </p>
    </div>
  );
}

