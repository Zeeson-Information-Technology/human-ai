export default function TalentPayment() {
  return (
    <div className="prose prose-slate max-w-none">
      <p>
        We pay fairly and on time — per task with difficulty weighting, or
        hourly for research/creation pipelines. Reviewers and leads have
        separate bands. Transparent earnings dashboards reduce churn and improve
        consistency.
      </p>
    </div>
  );
}
